'use strict';

export default new Map([
        [200, 'ok'],
        [201, 'created'],
        [204, 'no content'],
        [208, 'already reported'],
        [400, 'bad request'],
        [500, 'server error']
    ]);
